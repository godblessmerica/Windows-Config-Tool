# Windows-Config-Tool
A simple batch file that is used for people who don't have windows activated, it enables dark/light mode (temporarily), removes watermark (temporarily), and can completely bypass windows activation with MAS by massgravel.
